<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <Dialog
        id="approveDialog"
        title=""
        icon="">
        <content>
        </content>
        <beginButton>
            <Button
                text="{i18n>cancelButton}"
                press=".onCloseDialog" />
        </beginButton>
        <endButton>
            <Button
                text="{i18n>Approve}"
                type="Emphasized"
                press=".onApproveConfirm" />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
<mvc:View
    xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:macros="sap.fe.macros"
    xmlns:html="http://www.w3.org/1999/xhtml"
    controllerName="appiuimodule.controllers.InvoiceDetails">
    <Page
        id="InvoiceDetails"
        title="{i18n>invoiceDetailsTitle}"
        showNavButton="true"
        navButtonPress=".onNavBack">
        <content class="sapUiSmallMarginBottom sapUiMediumMarginBegin sapUiMediumMarginEnd">
            
            <!-- Invoice Details Table -->
            <Table
                id="invoiceTable"
                items="{ path: 'invoiceModel>/invoiceDetails' }"
                class="sapUiResponsiveMargin"
                width="auto">
                <headerToolbar>
                    <Toolbar>
                        <Title text="{i18n>invoiceDetailsTitle}" />
                        <ToolbarSpacer />
                        <core:Fragment fragmentName="appiuimodule.views.CustomerDetailsButton" type="XML" />
                    </Toolbar>
                </headerToolbar>
                <columns>
                    <Column width="8em">
                        <Text text="{i18n>fieldColumn}" />
                    </Column>
                    <Column>
                        <Text text="{i18n>valueColumn}" />
                    </Column>
                </columns>
                <items>
                    <ColumnListItem>
                        <cells>
                            <Text text="{invoiceModel>label}" />
                            <Text text="{invoiceModel>value}" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>

            <!-- Products Table -->
            <Table
                id="productsTable"
                items="{ path: 'productsModel>/products' }"
                class="sapUiResponsiveMargin sapUiMediumMarginTop"
                width="auto">
                <headerToolbar>
                    <Toolbar>
                        <Title text="{i18n>productsInOrderTitle}" />
                    </Toolbar>
                </headerToolbar>
                <columns>
                    <Column width="12em">
                        <Text text="{i18n>productNameColumn}" />
                    </Column>
                    <Column width="6em">
                        <Text text="{i18n>quantityLabel}" />
                    </Column>
                    <Column width="8em">
                        <Text text="{i18n>unitPriceLabel}" />
                    </Column>
                    <Column width="6em">
                        <Text text="{i18n>discountLabel}" />
                    </Column>
                    <Column width="8em">
                        <Text text="{i18n>extendedPriceLabel}" />
                    </Column>
                </columns>
                <items>
                    <ColumnListItem>
                        <cells>
                            <Text text="{productsModel>ProductName}" />
                            <Text text="{productsModel>Quantity}" />
                            <Text text="{
                                parts: ['productsModel>UnitPrice'],
                                formatter: '.formatCurrency'
                            }" />
                            <Text text="{
                                parts: ['productsModel>Discount'],
                                formatter: '.formatDiscount'
                            }" />
                            <Text text="{
                                parts: ['productsModel>ExtendedPrice'],
                                formatter: '.formatCurrency'
                            }" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>

        </content>
        <footer>
            <core:Fragment fragmentName="appiuimodule.views.Footer" type="XML" />
        </footer>
    </Page>
</mvc:View>